# Copy to .env.local and fill in.

# Required for the chat assistant. Without it the panel replies
# "The assistant isn't configured yet" instead of failing.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
