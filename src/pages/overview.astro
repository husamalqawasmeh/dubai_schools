---
/**
 * The old address, kept alive. The overview is an admin page now, so this is
 * also the way the dirham gesture reaches it — three clicks land here and the
 * middleware behind /admin decides whether you get the page or the sign-in
 * form.
 */
return Astro.redirect("/admin/overview", 301);
---
