---
/**
 * The old address, kept alive — same rename as the parents' copy. "supplies"
 * alone read as stock or stationery generally; the nav item has always called
 * it School Supplies.
 */
return Astro.redirect("/schools/school-supplies", 301);
---
