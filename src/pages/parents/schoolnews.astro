---
/**
 * The old address, kept alive — it is the URL the news approval emails have
 * been carrying, so anything already sent still lands.
 */
return Astro.redirect("/parents/school-news", 301);
---
