---
/**
 * The old address, kept alive. It has pointed at two different things — the
 * buying guide, then the supplier directory — and both of those now live at
 * school-supplies, so it redirects there rather than 404ing on anyone who
 * kept a link from either period.
 */
return Astro.redirect("/parents/school-supplies", 301);
---
