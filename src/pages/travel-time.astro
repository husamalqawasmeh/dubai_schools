---
/**
 * The old address, kept alive — same reason as /faq, /supplies and
 * /pick-of-the-week. Travel time is a parent's question, so the page moved
 * into the parents' section; this was in the nav for months, so it redirects
 * rather than disappearing.
 */
return Astro.redirect("/parents/travel-time", 301);
---
