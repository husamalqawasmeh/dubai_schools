---
/**
 * The old address, kept alive. Advertising is a school-facing offer, so the
 * page moved into the schools' section with the rest of them.
 */
return Astro.redirect("/schools/advertise", 301);
---
