---
/**
 * The old address, kept alive — same reason as /faq and /contribute. It was in
 * the nav and is the kind of page people bookmark, so it redirects rather than
 * disappearing.
 */
return Astro.redirect("/parents/pick-of-the-week", 301);
---
