---
/**
 * This page is gone, not moved.
 *
 * It was a third copy of the communicate form, reached from the nav, for
 * advertisers — but an advertiser is a school, and /schools/communicate was
 * already asking them the same questions. Two forms for one audience meant two
 * places for a reply to arrive and two to keep in step, so it folds into the
 * advertise page, which is what people were passing through it to reach.
 */
return Astro.redirect("/schools/advertise", 301);
---
