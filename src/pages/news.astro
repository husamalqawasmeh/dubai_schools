---
/**
 * The old address, kept alive — it was in the nav and it is the URL the
 * approval emails have been carrying, so anything already sent still lands.
 */
return Astro.redirect("/parents/school-news", 301);
---
