# dependencies
/node_modules

# build output
/dist/
/.astro/
/.wrangler/
*.tsbuildinfo

# generated seed SQL (regenerate with: npm run db:seed / db:seed:fees)
/.seed.sql
/.grade-fees.sql

# env files
.env*
!.env.example

# generated data exports (regenerate with: npm run export:schools)
/exports/

# local data backups must never be committed
*_DATA_BACKUP_*.zip

# misc
.DS_Store
*.pem
npm-debug.log*
/.admin.sql
/.emails.sql

# Kept beside the code but never committed: the Resend key lives in this one,
# and the two pictures are working references, not part of the app.
resend key.docx
images.jpg
world-travel-landmarks-concept-with-iconic-cities-halfearth_485374-25004.avif

# Scratch scripts used for one-off edits and checks. They are written, run and
# deleted in the same breath; one slipped into a commit, which is what this is for.
/*.py
/*.mjs
/*.tmp
